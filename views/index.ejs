<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        body{
            text-align: center;
        }
        table{width: 100%;
        border: 2px  solid black;
        }
        
    </style>
</head>
<body>
    <h1 style="color: blue;">Student Data</h1>
    <h3>    
        <!-- <%= title %> -->
    </h3>

    <h1>CRUD Operation</h1>
    <h4>Register <a href="/register">Register Here</a></h4>

    <% if(students!= "") { %>


    <table border=1px>
        <tr>
            <th>Roll no</th>
            <th>Name</th>
            <th>E-mail</th>
            <th>Age</th>
            <th>Operation</th>
            <% students.forEach((students, index) =>{ %>
            <tr>
                <td><%= index+1 %></td>
                <td><%= students.name %></td>
                <td><%= students.mail %></td>
                <td><%= students.age %></td>
                <!-- <td>Update | <a href="'/delete/<%= students._id %>">Delete</a>  </td> -->
<!-- Corrected hyperlink in the "Operation" column -->
<td> <a href="/edit/<%= students._id %>">Update</a>  | <a href="/delete/<%= students._id %>">Delete</a></td>



            </tr><% }) %>
        
    </table>
    <% } %>
</body>
</html>